# Essential dependencies for MoGe360 simplified
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.25.0
einops>=0.6.0
transformers>=4.28.0
safetensors>=0.4.0
Pillow>=10.0.0
trimesh>=4.3.0
huggingface-hub>=0.19.0
accelerate>=0.25.0
psutil>=5.9.0

# Optional dependencies (will be imported lazily if needed)
# matplotlib>=3.7.0  # Only needed for colorized depth visualization (not used in default simplified version)